# Data Crawling With Python Using Selenium

## Technologies:
Project is created with:
* python 3.11

## Setup
To run this project, install it locally using pip:
```
$ pip install -r requirements.txt
```